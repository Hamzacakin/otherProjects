function calFinalNote(vize1, vize2, final) {
    return vize1 * 0.15 + vize2 * 0.15 + final * 0.7;
    /* kodlar buraya */
  }
  
  /* Bu satırdan sonraki satırları silmeyin! */
  module.exports = calFinalNote;
  